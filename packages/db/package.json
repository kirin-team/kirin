{
  "name": "db",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "grafbase dev",
    "db:deploy": "grafbase deploy"
  },
  "devDependencies": {
    "@grafbase/sdk": "~0.1.0",
    "@types/node": "^18.17.1",
    "grafbase": "^0.29.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "tsconfig": "workspace:*"
  }
}
