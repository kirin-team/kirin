{
  "name": "db",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "devDependencies": {
    "@types/node": "^18.17.1",
    "prisma": "^5.1.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@prisma/client": "5.1.0",
    "tsconfig": "workspace:*"
  }
}
